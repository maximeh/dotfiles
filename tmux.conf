set -g default-terminal screen-256color
set -g history-limit 200000
set -g base-index 1
setw -g pane-base-index 1
set -sg escape-time 1
set-option -g prefix C-a

# enable scroll with the mouse
setw -g mode-mouse off

# reload config without killing server
bind r source-file ~/.tmux.conf \; display-message "  Config reloaded..".

# move x clipboard into tmux paste buffer
##CLIPBOARD selection integration
##Requires prefix key before the command key
#Copy tmux paste buffer to CLIPBOARD
bind C-c run "tmux show-buffer | xclip"
#Copy CLIPBOARD to tmux paste buffer and paste tmux paste buffer
bind C-v run "tmux set-buffer -- \"$(xclip -o -selection clipboard)\"; tmux paste-buffer"

unbind % # Remove default binding since we’re replacing
bind | split-window -h
bind _ split-window -v

# cycle through panes
unbind ^A
bind ^A select-pane -t :.+

#tile all windows
unbind =
bind = select-layout tiled

# border colours
set -g pane-border-fg colour205
set -g pane-active-border-fg colour205
set -g pane-active-border-bg default

# Set status bar
set -g window-status-format "#I:#W"
set -g window-status-current-format "#I:#W"

# default window title colors
set-window-option -g window-status-fg black
set-window-option -g window-status-bg default
set-window-option -g window-status-attr bright

# active window title colors
set-window-option -g window-status-current-fg white
set-window-option -g window-status-current-bg default
set-window-option -g window-status-current-attr bright

# command/message line colors
set -g message-fg white
set -g message-bg black
set -g message-attr bright

set -g status-interval 10
set-option -g status-utf8 on
set -g status-bg black
set -g status-fg green
set -g status-left-length 30
set -g status-left ' #[fg=colour166,bright]#H #[default] |'
set -g status-right-length 45
set -g status-right '| #(battery-life) (#(battery-time)) #[fg=white]%m-%d %H:%M '
set -g status-justify centre

setw -g monitor-activity on 
set -g visual-activity on

set-window-option -g window-status-current-bg colour239 
set-window-option -g window-status-current-fg white

bind E resize-pane -L 5 
bind U resize-pane -D 5 
bind H resize-pane -U 5 
bind T resize-pane -R 5
