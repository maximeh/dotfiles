[color]
	diff = auto
	status = auto
	branch = auto
	interactive = auto
	ui = 1
[gc]
	auto=1
[merge]
	summary=true
[alias]
	news = log -p HEAD@{1}..HEAD@{0}
	unstage = reset HEAD
	uncommit = reset --soft HEAD^
	count = shortlog -sn
	# Find every version of every file and sort by size; help find huge file
	hugefiles = !"for commit in $(git log --all --pretty=format:%H); do git ls-tree -r -l $commit; done |awk '{print $4 \" \" $5}' |sort -nu"
	# Will rewrite history and remove file
	delfile = !"sh -c \"git filter-branch -f --index-filter 'git rm --cached --ignore-unmatch $1' -- --all\""
	# Switch to a branch, creating it if necessary
	go = checkout -B
	# Show verbose output about tags, branches or remotes
	branches = branch -a
	remotes = remote -v
	# Clone a repository including all submodules
	c = clone --recursive
	# Commit all changes
	ca = !git add -A && git commit -av
	format-patch = format-patch -M
	# Show the diff between the latest commit and the current state
	d = !"git diff-index --quiet HEAD -- || clear; git diff --patch-with-stat"
	rb = "!sh -c \"git rebase -i --autostash --autosquash HEAD~$1\" -"
	gca = gc --aggressive --prune=now
	s = !"git status -sb"
	glog = log --oneline --graph --decorate
	slog = log --oneline --decorate
	plog = log -p --stat
	# stash shortcuts
	sl = stash list
	sa = stash apply
	ss = stash save
	ctags = !.git/hooks/ctags
	cleanup = !"rm -rf .git/refs/original/; git reflog expire --expire=now --all; git fsck --full --unreachable;  git repack -A -d; git gca"
[help]
	autocorrect = 1
[push]
	default = simple
[branch]
	autosetuprebase = always
[core]
	fsyncobjectfiles = yes
	excludesfile = ~/.gitignore_global
	autocrlf = input
[init]
	templatedir = ~/.git_template
[apply]
	whitespace = warn
[diff]
	renames = copies
	renamelimit = 2048
[gui]
	fontui = -family \"DejaVu Sans\" -size 16 -weight normal -slant roman -underline 0 -overstrike 0
	fontdiff = -family \"DejaVu Sans Mono\" -size 16 -weight normal -slant roman -underline 0 -overstrike 0
[include]
	path = ~/.dotfiles/gitconfig_sendemail
